import EmailForm from '@/components/EmailForm';

export default function LoginPage() {
  return (
    <main className="h-screen w-screen texture-bg flex justify-center items-center">
      <EmailForm />
    </main>
  );
}
